fastapi
uvicorn[standard]==0.30.6
python-telegram-bot==21.6
httpx==0.27.2
python-dotenv==1.0.1
supabase==2.7.4
# celery==5.4.0  # re-enable when pipeline is wired
# redis==5.0.8   # re-enable when Celery is re-enabled
